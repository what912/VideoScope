# VideoScope Repository Rules

本文件适用于整个仓库。Codex、其他自动化代理和贡献者都必须遵守。
子目录中的规则只能增加更具体的约束，不能放宽本文件的要求。

## 1. 开始修改前

修改前必须先阅读产品和架构文档。

修改任何文件前，必须先阅读：

1. `docs/product-spec.md`
2. `docs/architecture.md`
3. `docs/roadmap.md`
4. `docs/report-schema.md`

同时检查当前任务范围、仓库状态和相关测试。每次任务只实现提示词
明确要求的内容，不得借机加入未请求的功能、重构、依赖或发布工作。
产品范围与这些文档冲突时，必须停下并说明冲突，不能自行扩大范围。

## 2. 产品边界

- VideoScope 是 local-first、CPU-first 的诊断工具。
- v0.1 不生成或修复视频，不做人脸身份识别，不判断真实人物身份，
  不声称准确判断复杂物理规律。
- 不得把不同检测器分数合成为未经校准的总质量分。
- 镜头切换本身不是质量错误。检测器可以把切换作为上下文或排除区，
  不能仅因发生切换就产生质量 Finding。
- Finding 文案只描述可观察到的现象、使用的指标和启发式判断。不得
  把“疑似”“相对异常”写成已确定的故障、意图或客观事实。

## 3. 离线、模型和硬件

- 基础安装和基础测试不得访问互联网。
- 基础安装不得自动下载模型、权重、测试视频或其他运行时资产。
- 基础测试不得依赖 GPU，必须能在纯 CPU 环境运行。
- 网络必须在基础测试中被禁用或替换；不能以缓存恰好存在作为离线
  测试通过的依据。
- 可选 AI 模型必须延迟加载。未启用 AI 功能时，不得导入重型 AI
  运行时、初始化 provider、探测 GPU 或下载模型。
- 每个可选 AI provider 都必须提供无网络、无模型、结果可控的
  fake provider，用于单元测试和故障注入。

## 4. 外部命令和路径安全

- 所有外部命令必须使用参数数组调用，例如
  `subprocess.run(["ffprobe", ...], shell=False)`。
- 禁止 `shell=True`，禁止把用户路径拼接到命令字符串，禁止依赖
  shell 展开、重定向或管道。
- 检查返回码，限制或安全处理 stderr，并对超时和缺失可执行文件给出
  结构化错误。
- Windows、Linux 和 macOS 路径必须兼容。使用 `pathlib` 和平台无关
  API，不手写路径分隔符，不假定盘符、根目录或大小写规则。
- 测试必须覆盖包含空格、中文字符和非 ASCII 字符的输入、输出及临时
  目录路径。
- 报告只能使用输出目录内的相对产物路径，不能泄露个人绝对路径。

## 5. Detector 规则

每个 Detector 必须：

- 有独立、显式且可校验的配置模型；
- 有独立单元测试，包括正例、负例、边界和失败路径；
- 返回 `docs/report-schema.md` 定义的统一 Finding 结构；
- 在报告中记录稳定算法版本和全部生效参数；
- 只描述可观察到的现象，并公开分数的检测器内语义；
- 通过编排器交付结果，不得直接写 JSON 或 HTML；
- 失败时转换为 `detector_error`，不得伪装成零 Findings。

Detector 之间必须隔离：

- 一个 Detector 不得导入、实例化或调用另一个 Detector；
- 不得读取或修改另一个 Detector 的 Findings；
- 共享数据只能来自明确的只读分析上下文、稳定数据模型或声明的
  `feature_provider`；
- 依赖关系由编排器声明和注入，不能通过全局状态、文件副作用或调用
  顺序隐式建立。

所有检测阈值、合并窗口、最小时长、切镜保护窗口和严重程度边界都
必须来自配置模型。业务代码中不得散落魔法阈值；算法固有常量也要
命名、说明并与可调配置明确区分。

## 6. Finding 和报告

- 每个 Finding 必须包含合法时间区间、严重程度、检测器内分数、非空
  证据和解释。
- 视觉 Finding 必须包含证据帧或帧对。
- JSON 是事实来源；HTML 只能渲染已校验的报告模型，不能重新检测、
  补造 Findings 或改变结论。
- 相同输入、版本和完整配置必须产生确定性 Findings、证据选择、ID
  和排序。
- 单个 Detector 失败不能丢失其他结果；报告应为 `partial` 并记录
  `detector_error`。

## 7. 测试和反作弊

- 禁止为了让测试通过而根据测试文件名、固定哈希、样例路径或断言
  内容返回写死结果。
- 测试 fixture 可以有明确预期，但生产逻辑必须由输入数据和配置计算
  结果。
- 禁止降低、删除或跳过失败测试来掩盖回归，除非任务明确要求且说明
  理由。
- 不得伪造 Benchmark、运行结果、准确率、性能数字、日志或截图。
- Benchmark 必须记录数据清单、配置、版本、指标定义和实际执行条件。
- 未实际运行的命令必须标记为未运行；缺少工具或 fixture 时应报告
  阻塞，不能声称通过。

## 8. 统一验证

每次修改后必须运行仓库统一验证脚本：

```text
python scripts/validate.py
```

该脚本未来必须统一执行测试、静态检查、格式检查和类型检查，并保持
基础路径离线、CPU-only。当前脚本尚未在文档阶段创建；脚本不存在时，
必须明确报告“统一验证不可用”，运行本次修改能够执行的最小人工检查，
且不得声称统一验证通过。创建该脚本属于后续明确的工程任务，不能在
文档任务中擅自添加。

## 9. 版本控制与发布

未经用户或任务明确要求，Codex 和自动化代理不得：

- 执行 `git commit`；
- 执行 `git push`；
- 发布到 PyPI；
- 创建 GitHub Release；
- 创建或合并 Pull Request；
- 修改远程仓库、标签或发布资产。

允许在任务范围内读取 Git 状态和差异。提交前不得覆盖、丢弃或混入
用户的无关修改。

## 10. 完成任务时

必须报告：

- 实际修改的文件；
- 实际运行的验证及结果；
- 未运行或不可用的验证；
- 仍存在的风险或阻塞；
- 是否发生了外部访问、模型下载、提交或发布。

报告必须与实际工具输出一致。
