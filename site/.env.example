# Optional browser authentication. Leave both blank to keep sign-in unavailable
# while anonymous local analysis remains fully functional.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Sanitized report sharing is independently disabled unless set to true.
VITE_SUPABASE_SHARE_ENABLED=false
VITE_WINDOWS_INSTALLER_URL=
