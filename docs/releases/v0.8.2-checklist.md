# GenVideoScope v0.8.2 PREPARE-only checklist

Status: **PREPARE-ONLY; PUBLICATION PENDING**

Release version: `0.8.2`

Reserved tag: `v0.8.2` (not created)

Previous immutable release: `v0.8.1`

This checklist covers only the merged Video Rescue full-output flow correction
and its release metadata. It does not authorize a tag, GitHub Release, PyPI
upload, or any mutation of v0.8.1.

## Scope boundary

- [x] Runtime, dashboard, public-site, installer, smoke and citation version
  surfaces agree on `0.8.2` in the PREPARE branch.
- [x] README and Connector live install links remain pinned to the published
  v0.8.1 wheel until v0.8.2 publication is independently verified.
- [x] The delta is limited to persisted Rescue damage-map access and bounded
  stabilization timestamp-generation semantics already merged through PR #30.
- [x] Processing thresholds, dependencies, AnalysisReport/Rescue schemas,
  source immutability and local-first CPU behavior remain unchanged.

## PREPARE-branch validation

- [ ] Targeted release metadata tests pass.
- [ ] Dashboard `npm test` and `npm run build` pass.
- [ ] Public-site `npm audit --audit-level=high` and `npm run check` pass.
- [ ] `python scripts/validate.py` passes in a fresh no-clobber root with fixed
  FFmpeg/ffprobe 8.1.2.
- [ ] Independent review confirms the actual diff and PREPARE-only boundary.

## Post-merge exact-commit gates

These gates require the future exact merged `main` commit and are intentionally
pending in this PREPARE-only change:

- [ ] Dashboard `npm test` and `npm run build` pass.
- [ ] Public-site `npm audit --audit-level=high` and `npm run check` pass.
- [ ] `python scripts/validate.py` passes from the exact merged commit.
- [ ] `python -m build --no-isolation` produces only the 0.8.2 wheel and sdist.
- [ ] `python scripts/audit_distribution.py dist` passes both archives.
- [ ] The exact 0.8.2 wheel passes the offline base-wheel smoke.
- [ ] The Windows bundle audit, pinned Inno Setup installer build and
  install/start/stop/uninstall smoke pass for version 0.8.2.
- [ ] Independent review confirms the rebuilt asset and release boundary.
- [ ] Windows bundle LICENSE/NOTICE content and the Python/npm transitive
  license inventories are reviewed and frozen with the release evidence.
- [ ] `release-evidence.json` identifies the exact merged source commit and the
  exact wheel, sdist and installer hashes and sizes.
- [ ] `SHA256SUMS.txt` and `VideoScope-Setup-x64.exe.sha256` agree with the
  verified assets.

## Expected release assets

The six expected release files are:

- `genvideoscope-0.8.2-py3-none-any.whl`
- `genvideoscope-0.8.2.tar.gz`
- `VideoScope-Setup-x64.exe`
- `VideoScope-Setup-x64.exe.sha256`
- `SHA256SUMS.txt`
- `release-evidence.json`

Every checked result must be backed by retained output. Fixture-bounded
qualification is deterministic engineering evidence, not a claim of
real-world recovery or artistic quality.
