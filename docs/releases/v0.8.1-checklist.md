# GenVideoScope v0.8.1 candidate release checklist

Status: **PREPARE-ONLY; not tagged or published**

Candidate base: `6b6a9f0c448ee8b835e1cd0ff5939fc8f87856be`

Release version: `0.8.1`

Reserved tag: `v0.8.1` (must not be created until a separately authorized
release operation)

The published `v0.8.0` tag, release, assets, checklist and
[`release-audit.md`](../../release-audit.md) are immutable historical evidence.
This checklist records only the `v0.8.1` delta and its new verification. It does
not replace or rewrite the [`v0.8.0` checklist](../release-checklist.md).

## Candidate identity

- [x] The fresh worktree started at exact remote `main` commit
  `6b6a9f0c448ee8b835e1cd0ff5939fc8f87856be`.
- [x] The candidate branch is `codex/v0.8.1-prepare`.
- [x] Python, dashboard, public-site, installer, smoke and citation versions
  agree on `0.8.1`.
- [ ] The reviewed candidate commit is frozen and recorded here.
- [ ] The remote `v0.8.1` tag is confirmed absent immediately before release.

## Delta scope

- [x] Reproducible project-authored public cases and full local four-mode demo.
- [x] Bounded Video Rescue V15 qualification with unchanged pass thresholds.
- [x] Windows atomic-publication and validation-controller reliability fixes.
- [x] FFmpeg/ffprobe 8.1.2 CI pin and secure transitive `nanoid` lock update.
- [x] No report-schema migration, detector aggregate score, remote media
  processing, bundled FFmpeg/model, or source overwrite is introduced.

## Historical automated verification — stale for the current candidate

All commands must run from the candidate worktree, without network or model
downloads unless a later gate explicitly authorizes otherwise. The checked
results below are retained evidence for the earlier 2026-08-24 candidate state.
Historical PREPARE evidence commit: `a155c2cbdd14081682ea57493afc34b9d135f963`.
This evidence does not cover any later commit.
The tonal probe retry fix is committed at `912fe467192f615e9ad3f6c338fbd388ac1a065a`; the release-compliance changes are also committed in the current candidate history.
The retained results are stale and do not establish that the current candidate
is green, frozen or releasable.
Every applicable gate must be repeated from the exact commit that is eventually
frozen.

- [x] Targeted version, release-document and smoke tests pass.
- [x] `python scripts/validate.py` passes completely.
- [x] Dashboard `npm test` and `npm run build` pass.
- [x] Public site `npm audit --audit-level=high` and `npm run check` pass.
- [x] `python -m build --no-isolation` produces only the 0.8.1 wheel and sdist.
- [x] `python scripts/audit_distribution.py dist` passes both archives.
- [x] The exact 0.8.1 wheel passes the offline base-wheel smoke.
- [x] The Windows bundle audit, pinned Inno Setup 6.4.3 installer build and
  install/start/uninstall smoke pass for version 0.8.1.
- [x] `git diff --check` and final candidate boundary review pass.

## Retained preparation evidence — 2026-08-24

- Targeted release metadata/smoke tests passed: 27 tests. Targeted Ruff lint and
  format checks passed after normalizing the edited version lines.
- The first fresh unrestricted unified run used FFmpeg/ffprobe 8.1.2 and
  retained `2706 passed, 29 skipped, 11 failed`. Ten failures were preparation
  omissions: missing ignored generated fixtures or missing exact tonal tool
  environment variables. After generating all 29 fixtures, those exact
  privacy/publish/tonal nodes passed 16 tests and the isolated native Rescue
  suite passed all 25 tests. The remaining unified failure was a Windows
  `WinError 5` while publishing an absent Publish Ready output directory. A
  bounded retry now applies only to error 5 while the source exists and the
  destination remains absent; its targeted regression and later unified run
  pass.
- Dashboard verification passed 19 files / 116 tests and the production build.
- Public-site lint, typecheck, 64 files / 577 tests, 15-file offline media
  generation, production build, media verification and bundle budgets passed.
  The initial case verifier was blocked because Windows checkout changed the
  raw bytes of the three hashed `public-report.json` files from LF to CRLF.
  Their checkout is now fixed to canonical LF; all report, MP4 and WebP hashes
  match the manifest and the complete site check passes.
- `python -m build --no-isolation` produced
  `genvideoscope-0.8.1-py3-none-any.whl` and
  `genvideoscope-0.8.1.tar.gz`; both passed distribution audit. The exact wheel
  passed the offline base-wheel smoke with FFmpeg/ffprobe 8.1.2 across CPU
  Check, Publish Ready, Safe Sharing, both Rescue strategies and all three C
  goals.
- A fresh PyInstaller Windows Connector bundle passed the bundle audit and
  contained no FFmpeg, model or key assets. The official immutable Inno Setup
  6.4.3 asset was verified against release SHA-256
  `f3c42116542c4cc57263c5ba6c4feabfc49fe771f2f98a79d2f7628b8762723b`
  and a valid Authenticode signature, then expanded in official portable mode.
  The resulting unsigned installer has SHA-256
  `f7d36be7a36c30bdf8c3350149f0a2ddfc83e7bea7ba698f136796f5aef10c0c`;
  bundle audit and silent install, loopback health, controlled shutdown and
  uninstall smoke all pass without retained system state.
- The authorized read-only `npm audit --audit-level=high` reported zero
  vulnerabilities. The fresh complete site check passed lint, typecheck, 64
  files / 577 tests, offline media preparation, production build, media audit,
  bundle budgets and all three public case manifests.
- The corrected fresh unified run, with FFmpeg/ffprobe 8.1.2 and isolated
  cache/temp roots, passed Ruff, formatting, mypy, 2733 base tests with 21
  skips, and all 25 isolated native Rescue tests.

These historical PREPARE results remain useful diagnostics, but they are stale
for the current candidate, which contains later committed tonal and
release-compliance changes. The current candidate is **not release-ready**,
green or frozen.

## Required final sequence — current gates remain open

The release boundary is fail-closed and must proceed in this order:

1. [ ] Freeze and record one reviewed, clean candidate commit.
2. [ ] Rebuild every release asset and rerun the required automated, native,
   site, installer and human checks against that exact commit.
3. [ ] Run `scripts/release_assets.py prepare` into a fresh no-clobber root,
   then run its `verify` mode against the same commit. Retain
   `release-evidence.json`, `SHA256SUMS.txt` and the installer sidecar.
4. [ ] Under separate authorization, publish only the verified wheel, sdist,
   installer and three evidence/checksum files.
5. [ ] Only after publication, configure the exact official installer URL and
   its matching lowercase SHA-256 together; then switch public direct links.

## Human and external gates

These checks are not inferred from synthetic fixtures or automated CI:

- [ ] Maintainer confirms PyPI project ownership and final asset names.
- [ ] Resolved transitive licenses, `NOTICE`, and
  `docs/third-party-licenses.md` are reviewed for the candidate lockfiles.
- [ ] Authorized representative A/B/C/D media receives playback review in the
  intended browsers and players; the prior 32–36 second Video Rescue acceptance
  covers only that reviewed interval.
- [ ] Current Firefox and Chromium private-network pairing prompts are tested
  from the deployed HTTPS origin.
- [ ] Representative long-video CPU, memory and temporary-disk behavior is
  profiled, or the unverified scope remains explicit in the release notes.
- [ ] PowerShell and POSIX installation examples are checked on their target
  platforms.
- [ ] The unsigned Windows installer warning and independently generated
  SHA-256 remain visible; no signing claim is added.
- [ ] `release-evidence.json` identifies the exact frozen commit and the exact
  three primary asset hashes and sizes.

## Separate authorization boundaries

PREPARE-only completion does **not** authorize any of the following:

- [ ] Create a Git commit.
- [ ] Push a branch or create/merge a pull request.
- [ ] Create or move `v0.8.1`.
- [ ] Build/upload final GitHub Release assets or publish to PyPI.
- [ ] Mark the candidate notes as a final published release.

Every checked result must be backed by actual retained output. Failures remain
failures; do not modify and retry inside a single-run release gate.
