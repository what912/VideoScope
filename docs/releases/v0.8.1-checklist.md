# GenVideoScope v0.8.1 published release record

Status: **PUBLISHED AND IMMUTABLE**

Release version: `0.8.1`

Released: `2026-08-27`

Tag: `v0.8.1`

GitHub Release:
<https://github.com/what912/VideoScope/releases/tag/v0.8.1>

The published `v0.8.0` tag, release, assets, checklist and
[`release-audit.md`](../../release-audit.md) remain immutable historical
evidence. This record covers only the `v0.8.1` delta and does not replace the
[`v0.8.0` checklist](../release-checklist.md).

## Release identity and scope

- [x] Python, dashboard, public-site, installer, smoke and citation versions
  agree on `0.8.1`.
- [x] Reproducible project-authored public cases and the full local four-mode
  demo are included.
- [x] Bounded Video Rescue V15 qualification retains the reviewed pass
  thresholds.
- [x] Windows atomic-publication and validation-controller reliability fixes
  are included.
- [x] FFmpeg/ffprobe 8.1.2 is pinned for release-sensitive validation and the
  public-site lockfile contains the reviewed secure transitive dependency.
- [x] No report-schema migration, detector aggregate score, remote media
  processing, bundled FFmpeg/model, or source overwrite is introduced.

## Finalization-branch validation

The documentation finalization branch uses fresh no-clobber evidence roots.
The retained command output is the source of truth for these completed gates:

- [x] Targeted version, release-document and smoke tests pass.
- [x] `python scripts/validate.py` passes completely.
- [x] `git diff --check` passes.

## Published exact-commit gates

These gates were completed against the exact release commit before the
immutable tag and assets were published. Generated `release-evidence.json` is
the authoritative source commit, size and hash record:

- [x] Dashboard `npm test` and `npm run build` pass.
- [x] Public-site `npm audit --audit-level=high` and `npm run check` pass.
- [x] `python scripts/validate.py` passes from the exact merged commit.
- [x] `python -m build --no-isolation` produces only the 0.8.1 wheel and sdist.
- [x] `python scripts/audit_distribution.py dist` passes both archives.
- [x] The exact 0.8.1 wheel passes the offline base-wheel smoke.
- [x] The Windows bundle audit, pinned Inno Setup installer build and
  install/start/stop/uninstall smoke pass for version 0.8.1.
- [x] Independent review confirms the rebuilt asset and release boundary.

## Distribution, licenses and evidence

- [x] The Python distribution remains `genvideoscope`; import and CLI names
  remain `videoscope`.
- [x] Wheel, sdist and Windows bundle contents are audited against the release
  allowlists.
- [x] Windows bundle LICENSE/NOTICE content and the Python/npm transitive
  license inventories are reviewed and frozen with the release evidence.
- [x] The unsigned-installer warning is retained; no signing claim is made.
- [x] `release-evidence.json` identifies the exact merged source commit and the
  exact wheel, sdist and installer hashes and sizes.
- [x] `SHA256SUMS.txt` and `VideoScope-Setup-x64.exe.sha256` agree with the
  verified assets.

The exact generated hashes are not duplicated in this document.
`release-evidence.json` is authoritative for the frozen build identity. The
authoritative live publication state is supplied by GitHub Release and PyPI.

## Expected release assets

The six expected release files are:

- `genvideoscope-0.8.1-py3-none-any.whl`
- `genvideoscope-0.8.1.tar.gz`
- `VideoScope-Setup-x64.exe`
- `VideoScope-Setup-x64.exe.sha256`
- `SHA256SUMS.txt`
- `release-evidence.json`

## Human and external review

- [x] PyPI project ownership and the final distribution name are confirmed.
- [x] Resolved transitive licenses, `NOTICE`, and
  `docs/third-party-licenses.md` are reviewed.
- [x] The reviewed 32–36 second Video Rescue interval passed playback review
  for stabilization, frame edges and transition continuity. This bounded
  result is not generalized to arbitrary media.
- [x] The release notes retain the explicit limits on representative
  long-video, browser, player and real-world benchmark coverage.
- [x] PowerShell and POSIX installation examples identify the exact release.

## Publication record

1. The reviewed finalization change merged after required CI was green.
2. The six assets were rebuilt, verified and frozen from the exact `main`
   release commit.
3. The immutable `v0.8.1` tag and six-file GitHub Release were published.
4. The matching wheel and sdist were published to PyPI.
5. Live direct links now target the verified v0.8.1 assets; later releases do
   not modify this record.

Every checked result must be backed by actual retained output. Fixture-bounded
qualification is deterministic engineering evidence, not a claim of
real-world accuracy.
