# VideoScope v0.8.2 release notes

Status: **final release notes; publication pending**

Planned release: `0.8.2`

Reserved tag: `v0.8.2` (not created)

Previous immutable release: [`v0.8.1`](v0.8.1-checklist.md)

VideoScope v0.8.2 is a narrowly scoped Video Rescue reliability patch. It
packages the already merged full-output flow correction without changing the
reviewed processing thresholds, detector semantics, source-read-only contract,
or local-first CPU boundary.

## Fixed

- A reloaded Rescue job can serve its persisted damage map after the transient
  preparation object is no longer present, preventing a false “Damage map is
  not available” error during review.
- Stabilization generation still requires equal frame counts and exact
  timestamp inventories across the generated control and candidate siblings.
  The source parent retains its own timestamp digest instead of being required
  to share byte-identical normalized timestamps with re-encoded children.
- Confirmed processing can therefore reach the independently verified full
  faithful/improved output path. Bounded review previews remain intentionally
  short and are not presented as the final export.

## Compatibility and limits

- The Python distribution remains `genvideoscope`; the import and CLI remain
  `videoscope`.
- Python 3.11 and 3.12 remain supported.
- No AnalysisReport or Rescue schema migration is introduced.
- FFmpeg and ffprobe remain external prerequisites and are not bundled.
- Fixture and CI results are engineering evidence, not real-world recovery or
  artistic-quality claims.

## Verification record

The release workflow rebuilds and verifies every asset from the
exact merged `main` commit. Native validation uses fixed FFmpeg/ffprobe 8.1.2 and fresh
no-clobber logs, caches and temporary roots. The required record includes the
unified Python validation, dashboard and public-site checks, distribution
audit, exact-wheel offline smoke, Windows bundle audit, and installer
install/start/stop/uninstall smoke.

`release-evidence.json` binds the exact source commit to the primary wheel,
sdist and Windows installer hashes and sizes. `SHA256SUMS.txt` and the installer
sidecar provide the release checksums. These generated records, rather than
values copied into this document before the final build, are the authoritative
asset identities.

## Expected release assets

The verified v0.8.2 release set will contain exactly these six files:

- `genvideoscope-0.8.2-py3-none-any.whl`
- `genvideoscope-0.8.2.tar.gz`
- `VideoScope-Setup-x64.exe`
- `VideoScope-Setup-x64.exe.sha256`
- `SHA256SUMS.txt`
- `release-evidence.json`

Until all post-merge gates pass and publication is explicitly authorized,
README and Connector install surfaces continue to reference the immutable
v0.8.1 release. After publication is verified, public download surfaces must
switch only to the official v0.8.2 asset URLs and matching checksums.
