# VideoScope v0.8.1 release notes

Status: **draft candidate; not tagged or published**

Candidate base: `6b6a9f0c448ee8b835e1cd0ff5939fc8f87856be`

Previous immutable release: [`v0.8.0`](../release-checklist.md)

VideoScope v0.8.1 is a local-first stabilization and evidence release. It
packages the reviewed Video Rescue V15 qualification work, reproducible public
case studies and the full local four-mode demonstration while retaining the
existing source-read-only, preview, exact-confirmation and independent-
verification contracts.

## Highlights

- Adds bounded, measurement-gated Video Rescue qualification for tonal,
  deblur/sharpen clarity, audio denoise and transition stabilization candidates.
  A candidate that does not satisfy its unchanged thresholds falls back rather
  than being presented as an improvement.
- Adds three reproducible, project-authored public cases and a deterministic
  local Check/A/D/B/C demonstration contract. Provenance, source/result
  boundaries, media identities and public allowlists are verified explicitly.
- Refocuses the public site on observable rescue outcomes, with dedicated
  download, examples, developer, community, roadmap and case-study routes.
- Pins the release-sensitive native validation environment to FFmpeg/ffprobe
  8.1.2.

## Reliability and security

- Normalizes bounded Windows retry behavior for transient atomic-publication
  failures in privacy, rescue, content and release-validation paths. Retry
  budgets remain finite, non-transient failures fail immediately, and existing
  destinations are never silently overwritten.
- Preserves prefix-durable validation evidence and fresh no-clobber audit roots
  so a failed gate remains inspectable.
- Updates the public-site lockfile to a secure transitive `nanoid` version while
  leaving `site/package.json` dependency declarations unchanged.
- Waits for Publish Ready SSE subscription registration in the frontend test
  before delivering the synthetic callback, removing a Windows CI race without
  weakening the production assertion.

## Compatibility

- The Python distribution remains `genvideoscope`; the Python import and CLI
  remain `videoscope`.
- Python 3.11 and 3.12 remain supported.
- The v0.1 `AnalysisReport` schema remains `0.1`; this release does not migrate
  or reinterpret existing Findings.
- Base installation remains CPU-first, offline-capable and model-free. FFmpeg
  and ffprobe remain external executables and are not bundled in the wheel.

## Candidate validation status

The automated PREPARE gates are green. The local candidate run validated the
0.8.1 wheel/sdist identity, distribution audit, exact offline wheel smoke,
dashboard tests/build, complete public-site check, Windows Connector bundle
audit, pinned Inno Setup 6.4.3 installer build and install/start/uninstall
smoke. The final fresh unified run passed Ruff, formatting, mypy, 2733 base
tests with 21 skips, and all 25 isolated native Rescue tests.

The two Windows reliability blockers found during preparation are closed:

- Publish Ready no-replace directory publication now applies a finite retry
  only to transient error 5 while the source exists and the destination stays
  absent. Collisions, other errors and exhausted budgets still fail closed.
- The three hashed public case reports are fixed to canonical LF checkout;
  their report hashes and all case media/poster hashes match the manifest.

This remains a draft candidate rather than a release result. It must not be
tagged or published until a reviewed candidate commit is frozen, remaining
human/external gates are resolved or explicitly scoped, and a separate release
operation is authorized. Exact retained results are recorded in
[`v0.8.1-checklist.md`](v0.8.1-checklist.md).

## Known limitations

- Video Rescue qualification is heuristic and fixture-bounded. It cannot
  reconstruct information absent from the source, and a technically passing
  output is not proof of artistic quality.
- Project-authored synthetic cases establish deterministic engineering
  behavior, not accuracy or usefulness on arbitrary user media.
- Representative long-video, private-network browser, resource profiling and
  cross-platform player checks remain explicit human gates in the candidate
  checklist.
- The Windows installer remains unsigned; users must retain the documented
  unknown-publisher warning and verify its separately published SHA-256.

## Candidate artifacts

If a later, separately authorized release gate passes, the expected assets are:

- `genvideoscope-0.8.1-py3-none-any.whl`
- `genvideoscope-0.8.1.tar.gz`
- `VideoScope-Setup-x64.exe`
- `VideoScope-Setup-x64.exe.sha256`
- `SHA256SUMS.txt`

No asset, Git tag, GitHub Release or PyPI publication is created by this
PREPARE-only document.
